# Fake malloc (my_malloc)  
This is my attempt at trying to make a memory allocator in C. It should mimic the function in stdlib.h malloc(). This is probably very bad and not working properly.

TODO

- [ ] Connect two free memory blocks to avoid fragmentation.
